
{
  "name": "dukkan-chatbot-vercel",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend and Backend for the Dukkan Smart Chatbot on Vercel.",
  "scripts": {
    "dev": "echo 'This is a frontend-only project for local viewing. Run with a static server. The backend is deployed via Vercel.'",
    "build": "echo 'No build step required for the frontend. Vercel handles the backend build.'"
  },
  "dependencies": {
    "@google/genai": "^0.16.0",
    "@vercel/node": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10"
  }
}
